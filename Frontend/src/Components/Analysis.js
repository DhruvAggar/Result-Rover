import { Box, Button, Paper, Typography } from "@mui/material";
import React from "react";

function Analysis() {
  return (
    <Box sx={{ mt: 3 }}>
      <Paper elevation={1} sx={{ p: 3 }}>
        <Typography variant="h2">No Analysis Found</Typography>
      </Paper>
    </Box>
  );
}

export default Analysis;
